# API routes

Only integration boundaries, webhooks, and narrowly scoped application commands
belong here. Server Components and Server Actions remain the default application
entry points. Every route validates authentication, authorization, and input.
